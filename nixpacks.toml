[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["poetry config virtualenvs.create false", "poetry install --no-dev"]

[start]
cmd = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"